version: "3"

services:
  fastapi:
    image: app
    ports:
      - "8000:80"
    depends_on:
      pgdb:
        condition: service_healthy
    networks:
      - app-tier
  pgdb:
    image: postgis/postgis
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: mydb
      PGDATA: /var/lib/postgresql/data/my_data/
    networks:
      - app-tier
    volumes:
      - ./db:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d mydb -U admin" ]
      interval: 5s
      timeout: 5s
      retries: 5
  redis:
    image: redis
    ports:
      - 6379
    networks:
      - app-tier
    depends_on:
      pgdb:
        condition: service_healthy
networks:
  app-tier:
    driver: bridge